#ifndef _ANALYSIS_H_
#define _ANALYSSI_H_

#include "student_info.h"

bool compare_name(const core&, const core&);
bool compare_grade(const core&, const core&);
bool compare_name_ptr(const core*, const core*);

#endif
